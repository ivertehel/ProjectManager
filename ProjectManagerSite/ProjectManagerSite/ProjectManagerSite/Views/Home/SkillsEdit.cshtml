@model ProjectManagerSite.Models.SkillsVM

@foreach (var skill in Model.AllSkills)
{
    if (Model.UserSkills.FirstOrDefault(item => item == skill) != null)
    {
        <input type="checkbox" checked>@skill<br>
    }
    else
    {
        <input type="checkbox">@skill<br>
    }
}
