@model ProjectManagerSite.Models.SkillsVM

@for (int i = 0; i < Model.CheckBoxes.Count; i++)
{
    @Html.CheckBoxFor(item => item.CheckBoxes[i].Enabled) @Model.CheckBoxes[i].Name <br />;
    @Html.HiddenFor(item=>item.CheckBoxes[i].Name)
    
}
